[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "crowdfundr"
version = "0.1.0"
description = "Educational crowdfunding database project using PostgreSQL and MongoDB."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "psycopg2-binary>=2.9",
]

[tool.setuptools]
py-modules = []
